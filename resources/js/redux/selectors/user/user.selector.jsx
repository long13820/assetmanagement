export const isAddSelector = (state) => state.user.isAdd;
export const isEditSelector = (state) => state.user.isEdit;
export const userDetailSelector = (state) => state.user.user;
export const keyUserSelector = (state) => state.user.key;
